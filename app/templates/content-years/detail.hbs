<h2>{{this.model.name}}</h2>

{{#each this.model.subjects as |subject|}}
  <PaperCard>
    <PaperToolbar>
      <PaperToolbarTools>
        <h2>{{subject.name}}</h2>
      </PaperToolbarTools>
    </PaperToolbar>
    <PaperContent>
      <PaperList>
        {{#each (get this.contentsGroupedBySubjectId subject.id) as |content|}}
          <PaperItem>
            <p>{{content.notes}}</p>
          </PaperItem>
          <PaperDivider />
        {{/each}}
      </PaperList>
    </PaperContent>
  </PaperCard>
{{/each}}
